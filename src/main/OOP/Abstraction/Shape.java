package main.OOP.Abstraction;

// Clasă abstractă
public abstract class Shape {
    /*
    Abstractizarea înseamnă simplificarea unui concept complex prin definirea unei interfețe generale,
    care este apoi implementată de clase concrete. În Java, abstractizarea se realizează folosind clase abstracte
    și interfețe.
     */

    // Metodă abstractă
    public abstract double arie();

    // set a new color for each type of object (circle, triangle)
    // public abstract void setColor();

    // employee -> developer, manager
    // public abstract double getBonus(); // + 1 salary, 2-5 max salary
    // method for developer
//        {
//            if (employee.getTypeEmployee.equals("Developer") {
//                    newSalary = salary + bonus
//        }
    //method for manager
//        if(employee.getType.equals("Manager")&(employee.getYearsOfExperience==2))
//    {
//        new salary = salary + 2 * bonus;
//    }

}

